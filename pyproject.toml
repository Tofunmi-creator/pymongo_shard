[project]
name = "pymongo_shard"
version = "0.1.0"
description = "A python package for sharding pymongo collection documents. Do"
readme = "README.md"
authors = [
  {name = "Oluwatofunm Caulcrick", email = "johncaul@yahoo.com"}
]
maintainers = [
  {name = "Oluwatofunm Caulcrick", email = "johncaul@yahoo.com"}
]
classifiers = [
    # TODO
]
license = {text = "MIT"}
dependencies = [
  "typer"
]
requires-python = ">= 3.10"

[project.optional-dependencies]
test = [
    "coverage",  # testing
    "pytest",  # testing
    "ruff",  # linting
    "ty", # checking types
    "ipdb", # debugging
]

[project.urls]
bugs = "https://github.com/Tofunmi-creator/pymongo_shard/issues"
changelog = "https://github.com/Tofunmi-creator/pymongo_shard/blob/master/changelog.md"
homepage = "https://github.com/Tofunmi-creator/pymongo_shard"

[project.scripts]
pymongo_shard = "pymongo_shard.cli:app"

[tool.ty]
# All rules are enabled as "error" by default; no need to specify unless overriding.
# Example override: relax a rule for the entire project (uncomment if needed).
# rules.TY015 = "warn"  # For invalid-argument-type, warn instead of error.

[tool.ruff]
line-length = 120

[tool.ruff.lint]
select = [
    "E",   # pycodestyle errors
    "W",   # pycodestyle warnings
    "F",   # Pyflakes
    "I",   # isort
    "B",   # flake8-bugbear
    "UP",  # pyupgrade
]

[tool.uv]
package = true
